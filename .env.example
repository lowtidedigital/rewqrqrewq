# Environment variables for Link Harbour frontend
# Copy to .env.local and fill in values from Terraform outputs

# API Gateway endpoint (from terraform output api_url)
# NOTE: No /prod suffix - API Gateway uses $default stage
VITE_API_URL=https://iq0f9fazt9.execute-api.us-east-1.amazonaws.com

# Cognito configuration (from terraform outputs)
VITE_COGNITO_REGION=us-east-1
VITE_COGNITO_USER_POOL_ID=us-east-1_OK4RL4aI1
VITE_COGNITO_CLIENT_ID=64rars8ja9q3sbgc8a12ock8fk

# Short domain for redirect URLs (lh.linkharbour.io)
# This is where short links resolve: https://lh.linkharbour.io/r/{slug}
VITE_SHORT_DOMAIN=lh.linkharbour.io

# App domain for the main application
VITE_APP_DOMAIN=app.linkharbour.io
