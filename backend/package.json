{
  "name": "linkharbour-backend",
  "version": "1.0.0",
  "description": "linkharbour URL Shortener - AWS Lambda Backend",
  "scripts": {
    "build": "npm run clean && npm run compile && npm run package",
    "clean": "rimraf dist",
    "compile": "tsc",
    "package": "node scripts/package.js",
    "test": "vitest",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/client-s3": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "@aws-sdk/s3-request-presigner": "^3.500.0",
    "qrcode": "^1.5.3",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.10.0",
    "@types/qrcode": "^1.5.5",
    "@types/uuid": "^9.0.7",
    "archiver": "^6.0.1",
    "esbuild": "^0.27.2",
    "rimraf": "^6.1.2",
    "typescript": "^5.3.2",
    "vitest": "^4.0.16"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
